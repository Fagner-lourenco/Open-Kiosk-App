<?xml version="1.0" encoding="utf-8"?>
<!--
  Network Security Configuration for Open Kiosk App
  
  This configuration:
  1. Allows cleartext (HTTP) traffic only to localhost for development
  2. Enforces HTTPS for all other connections
  3. Uses system trust anchors (default CA certificates)
-->
<network-security-config>
    <!-- Base config: require HTTPS by default -->
    <base-config cleartextTrafficPermitted="false">
        <trust-anchors>
            <certificates src="system" />
        </trust-anchors>
    </base-config>
    
    <!-- Allow cleartext for localhost (development) -->
    <domain-config cleartextTrafficPermitted="true">
        <domain includeSubdomains="true">localhost</domain>
        <domain includeSubdomains="true">10.0.2.2</domain> <!-- Android emulator localhost -->
        <domain includeSubdomains="true">127.0.0.1</domain>
    </domain-config>
    
    <!-- Firebase domains (HTTPS enforced) -->
    <domain-config cleartextTrafficPermitted="false">
        <domain includeSubdomains="true">firebaseio.com</domain>
        <domain includeSubdomains="true">googleapis.com</domain>
        <domain includeSubdomains="true">firebase.google.com</domain>
        <domain includeSubdomains="true">firebaseapp.com</domain>
    </domain-config>
    
    <!-- ESP32 local network (may need cleartext for local devices) -->
    <domain-config cleartextTrafficPermitted="true">
        <domain includeSubdomains="true">192.168.0.0</domain>
        <domain includeSubdomains="true">192.168.1.0</domain>
        <domain includeSubdomains="true">192.168.4.1</domain> <!-- ESP32 AP default IP -->
    </domain-config>
</network-security-config>
