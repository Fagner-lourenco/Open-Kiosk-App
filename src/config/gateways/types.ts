/**
 * Gateway Registry — Types (re-export from shared)
 *
 * Single source of truth: shared/config/gateways/types.ts
 * Este arquivo apenas re-exporta para manter o path @/config/gateways/types funcional.
 */

export type {
  GatewayId,
  GatewayConfigField,
  GatewayStatus,
  GatewayDefinition,
} from '../../../shared/config/gateways/types';
